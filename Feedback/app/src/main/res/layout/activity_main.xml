<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MainActivity">

    <Button
        android:hapticFeedbackEnabled="true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Tap to Vibrate"
        android:id="@+id/myView"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="170dp" />
    <Button
        android:hapticFeedbackEnabled="true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Tap to Vibrate after Holding Down"
        android:id="@+id/myView2"
        android:layout_below="@+id/myView3"
        android:layout_alignRight="@+id/myView3"
        android:layout_alignEnd="@+id/myView3" />

    <Button
        android:hapticFeedbackEnabled="true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Tap to Vibrate Forever"
        android:id="@+id/myView3"
        android:layout_centerVertical="true"
        android:layout_alignLeft="@+id/myView"
        android:layout_alignStart="@+id/myView" />

</RelativeLayout>
